CREATE DATABASE covid_db;
USE covid_db;
CREATE TABLE covid_cases (
    id INT AUTO_INCREMENT PRIMARY KEY,
    date DATE,
    time TIME,
    state VARCHAR(50),
    confirmed_indian_national INT,
    confirmed_foreign_national INT,
    recoveries INT,
    deaths INT,
    confirmed_cases INT
);



Insert Initial COVID Data (DML)

INSERT INTO covid_cases
(date, time, state, confirmed_indian_national,
confirmed_foreign_national, recoveries, deaths, confirmed_cases)
VALUES
('2020-03-15', '10:00:00', 'India', 90, 10, 50, 2, 100);

Update Case Information (DML)

UPDATE covid_cases
SET confirmed_cases = 120
WHERE state = 'India'
AND date = '2020-03-15';


Delete incorrect records

DELETE FROM covid_cases
WHERE state = 'IN';


Alter Table (DDL)
Add a new column vaccination_rate.

ALTER TABLE covid_cases
ADD vaccination_rate FLOAT;
Update Vaccination Rate (Optional)
UPDATE covid_cases
SET vaccination_rate = 78.5
WHERE state = 'India';



Display Table
SELECT * FROM covid_cases;